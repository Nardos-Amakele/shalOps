import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        josephin: "var(--font-joseph-sans)", 
      },
    },
  },
  plugins: [],
};
export default config;
